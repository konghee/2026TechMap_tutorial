import SwiftUI
import RealityKit
import AquariumContent

struct AquariumView: View {
    var body: some View {
        RealityView { content in
            // 여기에 3D 콘텐츠를 추가합니다.
        }
    }
}

#Preview {
    AquariumView()
}
